import styles from './Gallery.module.css'

function Gallery () {
    return(
        <div className={styles.Gallery}>
            <h1>Gallery</h1>
        </div>    
        );
}

export default Gallery;
